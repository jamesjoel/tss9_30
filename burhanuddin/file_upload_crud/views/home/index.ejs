<div class="jumbotron text-center">
	<h1>Home</h1> 
</div>

<div class="container">
	<div class="row">
		<div class="col-lg-6">
			<form action="/" method="post" enctype="multipart/form-data">	
				<div class="card">
					<div class="card-header">Add user</div>
					<div class="card-body">
						<div class="form-group">
							<div class="custom-file">
								<label for="photo" class="custom-file-label">Photo</label>
								<input type="file" class="custom-file-input" id="photo" name="photo" />
							</div>
						</div>
						<div class="form-group">
							<label for="username">Name</label>
							<input type="text" class="form-control" name="username" id="username" />
						</div>
					</div>
					<div class="card-footer">
						<input type="submit" value="ADD" class="btn btn-success">
					</div>
				</div>
			</form>
		</div>
		<div class="col-lg-6">
			<table class="table table-stripped table-bordered table-hover">
				<thead>
					<tr>
						<th>S.No.</th>
						<th>Image</th>
						<th>Name</th>
						<th>Edit</th>
						<th>Delete</th>
					</tr>
				</thead>
				<tbody>
					<% result.forEach(function(item, index) { %>
					<tr>
						<td><%= index+1 %></td>
						<td><img src="/images/<%= item.photo %>" height="75" /></td>
						<td><%= item.username %></td>
						<td><a href="/edit/<%= item._id %>"><i class="fa fa-pencil"></i></a></td>
						<td><a href="/delete/<%= item._id %>"><i class="fa fa-close"></i></a></td>
					</tr>
					<% }); %>
				</tbody>
			</table>
		</div>
	</div>
</div>

<!--
<div class="custom-file-upload-box">
	<label for="file-upload" class="custom-file-upload"><img src="" /></label>
	<input id="file-upload" name="upload_img" type="file" style="display:none;">
</div>
<script>
	function readURL(input) {
		if (input.files && input.files[0]) {
			var reader = new FileReader();
			reader.onload = function(e) {
				$('#uploaded-img-preview').attr('src', e.target.result);
			}
			reader.readAsDataURL(input.files[0]);
		}
	}
	$("#file-upload").change(function() {
		readURL(this);
	});
</script>
-->